apiVersion: 1

providers:
  - name: Gateway
    orgId: 1
    folder: SMP Processing
    type: file
    disableDeletion: false
    editable: true
    updateIntervalSeconds: 30
    options:
      path: /etc/grafana/provisioning/dashboards
